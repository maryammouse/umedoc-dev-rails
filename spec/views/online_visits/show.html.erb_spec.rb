require 'rails_helper'

describe "/online_visits/show.html.erb", focus:true do
 # it "allows doctors to view upcoming/recent visits" do
    #oncall_time = create(:oncall_time)
    #past_visit = create(:visit, oncall_time_id: oncall_time.id,
                       #timerange: (Time.now + 1.minute)..(Time.now + 31.minutes))
    #upcoming_visit = create(:visit, oncall_time_id: oncall_time.id,
                           #timerange: (Time.now + 60.minutes)..(Time.now + 90.minutes))
    #session[:user_id] = oncall_time.doctor.user.id
    
    #render 

    #expect(rendered).to have_content "You have an upcoming visit!"
    #expect(rendered).to have_content "Your last visit was"
  end
